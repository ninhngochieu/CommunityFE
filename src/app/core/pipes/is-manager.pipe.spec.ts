import { IsManagerPipe } from './is-manager.pipe';

describe('IsManagerPipe', () => {
  it('create an instance', () => {
    const pipe = new IsManagerPipe();
    expect(pipe).toBeTruthy();
  });
});
